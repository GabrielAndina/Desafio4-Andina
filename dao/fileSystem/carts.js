const fs = require('fs');
const path = require('path');

const cartsFilePath = path.join(__dirname, '..', 'data', 'carts.json');

// Funciones para interactuar con los datos de los carritos en el sistema de archivos
